const {
  getAgrupadores,
  getAgrupadoresPorID
} = require('./agrupador');

const {
  getElementosPorIDAgrupador
} = require('./elemento');

module.exports = {
  getAgrupadores,
  getAgrupadoresPorID,
  getElementosPorIDAgrupador,
};